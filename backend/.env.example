# ============================================
# REQUIRED - Database & Security
# ============================================

# PostgreSQL Database (Supabase recommended)
DATABASE_URL=postgresql+psycopg2://user:password@host:port/database

# JWT Secret (generate with: openssl rand -hex 32)
JWT_SECRET=your-secret-key-min-32-characters

# Settings Encryption Key (generate with: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())")
SETTINGS_ENCRYPTION_KEY=your-fernet-encryption-key

# ============================================
# OPTIONAL - Additional Services
# ============================================

# Redis (optional - for caching)
# REDIS_URL=redis://localhost:6379/0

# CORS Origins (comma-separated)
BACKEND_CORS_ORIGINS=http://localhost:3000,http://localhost:8000

# ============================================
# NOTES
# ============================================
# 1. Copy this file to .env and fill in your values
# 2. AI Provider, Email, and other settings can be configured via Admin Panel (/admin/settings)
# 3. Never commit .env file to version control
